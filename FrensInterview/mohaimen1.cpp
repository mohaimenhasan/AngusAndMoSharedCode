class Solution1 {
public:
  vector<int> listProbability(vector<string> things, vector<int> numbers){
    int total = 0;
    for(int i=0; i < numbers.size(); i++){
      total += number[i];
    }
    vector<int> result;
    for(int i; i < numbers.size(); i++){
      result.push_back(numbers[i]/total);
    }
    return outpput;
  }
};


class Solution2{
public:
  vector<string> returnSameDistinct(vector<string> givenStrings){
    
  }
};
