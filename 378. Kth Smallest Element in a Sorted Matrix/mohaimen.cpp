class Solution {
public:
    int kthSmallest(vector<vector<int>>& matrix, int k) {
      int n = matrix.size();
      int iter = 0;
      int row = 0;
      int col = 0;
      int smallest;
      while (iter != k){
        vector<int> temp;
        temp = matrix[iter];

      }
    }
};
